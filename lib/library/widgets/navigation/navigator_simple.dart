// Core
import 'package:flutter/material.dart';

// Instruments
import 'page1.dart';

class NavigatorSimple extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Page1(),
    );
  }
}
