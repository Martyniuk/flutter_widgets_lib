// Core
import 'package:flutter/material.dart';

class FlutterBoringShowMain extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('Hello from Flutter Boring show'),
      ),
    );
  }
}
